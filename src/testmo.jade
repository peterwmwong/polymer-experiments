!!!
html(lang='en')
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    meta(name='viewport', content='width=320.1, initial-scale=1, user-scalable=no')
    meta(name='apple-mobile-web-app-capable', content='yes')
    meta(name='apple-mobile-web-app-status-bar-style', content='black')
    title TEST
    link(rel='stylesheet', href='testmo.css')

  body
    #panel.animateTransform yolo
    //- #panel.animateTop yolo

    :coffeescript
      setTimeout (->
        document.querySelector('#panel').classList.add 's-doit'

        ###
        ###
        setTimeout (->
          s = ""
          for i in [0...3000000]
            s += "#{i}"
        ), 200

        setTimeout (->
          document.querySelector('#panel').classList.remove 's-doit'
        ), 1000

      ), 100



    //- livereload
    script.
      document.write('<script src="http://' + (location.host || 'localhost').split(':')[0] + ':35729/livereload.js?snipver=1"></' + 'script>')
    script(src='../vendor/polymer-all/web-animations-js/web-animations.js')
    